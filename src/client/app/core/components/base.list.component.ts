
import { HelperService } from "../../core";

//abstract class EgritosComponent<TPrimaryKey> {
export abstract class BaseListComponent {
    //properties
    public title: string;

    constructor(protected _helperService: HelperService) { }

    
}