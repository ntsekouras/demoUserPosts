import {Injectable} from '@angular/core';

@Injectable()
export class CookieService {

    getCookieVa(key: string) {
    //    var value = "; " + document.cookie;
    //    var parts = value.split("; " + name + "=");
    //    if (parts.length == 2)
    //        return parseInt(parts.pop().split(";").shift());

    //    return null;
    }
}